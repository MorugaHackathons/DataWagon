// FilterModal.tsx
import React from 'react';
import styles from './FilterModal.module.scss';

const FilterModal: React.FC = () => {
    return <div className={styles.filterModal}>This is the filter modal.</div>;
};

export default FilterModal;
